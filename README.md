# webpack tsx template

* to **install** run:
    ```
    npm install
    ```

* to run **webpack development server**
    ```
    npm run dev-server
    ```

* to build in **development mode** run:
    ```
    npm run dev
    ```

* to build in **production mode** run:
    ```
    npm run build
    ```
